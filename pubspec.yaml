name: aahar_ai
description: A privacy-first, AI-powered nutrition tracker.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  flutter_dotenv: ^5.1.0
  intl: ^0.19.0
  path_provider: ^2.1.2
  supabase_flutter: ^2.3.0
  
  # --- NAVIGATION (You were missing this!) ---
  go_router: ^14.0.0

  # --- STABLE ISAR V3 ---
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.9

  # --- STABLE GENERATOR V3 ---
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true
  assets:
    - .env